/**
 * Es un nuevo metodo que se incorporo para la manipulacion de los
 * arreglos en javascript, entonces para hacer uso de este metodo
 * es igual que usar en metodo normal, primero colocamos un punto
 * despues de la variable que contenga el arreglo y luego colocamos
 * el nombre de at, este recibe un parametro en el cual le indicamos
 * el indice al cual queremos acceder su valor.
 * 
 */
const nums = [1, 2, 3, 4, 5, 6]
console.log(
    nums.at(0),
    nums.at(-1)
)